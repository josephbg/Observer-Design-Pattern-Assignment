/**
 * The Observer interface gives the different characters the ability to update their sayings based on the warning type 
 * @author Brian Guthrie
 *
 */
public interface Observer {
	public void update(int warning);
}
